---
draft: true
title: 一些脚本
date: 2026-04-15
lastmod: 2026-05-08
featured:
tags:
  - 计算机
  - 脚本
---


# 修改GIF宽高
```python
from PIL import Image

img = Image.open("input.gif")
img = img.resize((400, 600))
img.save("output.gif")
```

# 拼接GIF每一帧
```python
from PIL import Image
import numpy as np

img = Image.open("flag.gif")

frames = []
try:
    i = 0
    while True:
        img.seek(i)
        frames.append(img.convert("RGB"))
        i += 1
except EOFError:
    pass

arrays = [np.array(f) for f in frames]

h = arrays[0].shape[0]
w_total = sum(a.shape[1] for a in arrays)

canvas = np.zeros((h, w_total, 3), dtype=np.uint8)

x = 0
for a in arrays:
    canvas[:, x:x+a.shape[1]] = a
    x += a.shape[1]

Image.fromarray(canvas).save("flag.png")
```

# 倒转文件
```python
a = open('flag.jpg','rb')  #读取flag.jpg图片的byte数据
b = open('png.png','wb')  #新建一个名为png.png的图片，写入byte数据
b = b.write(a.read()[::-1])  #将flag.jpg图片的byte数据，倒着写入png.png图片里
```